services:
  blog:
    build:
      context: .
      dockerfile: Dockerfile
    image: tb-md-timeline-blog:latest
    container_name: tb-md-timeline-blog
    ports:
      - '4173:4173'
    environment:
      - NODE_ENV=production
      - PORT=4173
      - HOST=0.0.0.0
    restart: unless-stopped
    healthcheck:
      test:
        [
          'CMD',
          'bun',
          '--eval',
          "fetch('http://localhost:4173').then(() => process.exit(0)).catch(() => process.exit(1))"
        ]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.5'
          memory: 256M
    labels:
      - "xyz.atsignhandle.tb.app=blog"
      - "xyz.atsignhandle.tb.env=production"
      - "xyz.atsignhandle.tb.version=1.0"
    networks:
      - md-timeline-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  md-timeline-network:
    driver: bridge
